---
layout: base.njk
title: SISK - Projekty
---

<section class="section">
  <div class="container">
    <h1 class="title">Wrzystkie Projekty</h1>
    <ul>
      {% for post in collections.projects | reverse %}
          <a href="/projects/{{ post.fileSlug }}/" class="card-link">
            <div class="card">
            <progress class="progress" value="{{ post.data.progress }}" max="100" style="--bulma-progress-value-background-color:#{{post.data.color}}">
  {{ post.progress }}%
</progress>
              <div class="card-content">
                <p class="title is-4">{{ post.data.title }}</p>
                <p class="subtitle is-6">Kierownik: {{ post.data.projectCordinator }}</p>
                <div class="content">
                  <p><strong>Dates:</strong> {{ post.data.startDate }} - {{ post.data.endDate }}</p>
                  <p>{{ post.data.description }}</p>
                  {% if post.data.funding is defined %}
                  <p><strong>Funding:</strong> {{ post.data.funding }}</p>
                  {% endif %}
                </div>
              </div>
            </div>
          </a>
      {% endfor %}
    </ul>
  </div>
</section>
